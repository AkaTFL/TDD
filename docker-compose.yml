version: '3.8'

services:
  web:
    build: .
    container_name: walli-web
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
      - ./data:/var/www/html/data
    environment:
      - PHP_ENV=development
    networks:
      - walli-network

networks:
  walli-network:
    driver: bridge

